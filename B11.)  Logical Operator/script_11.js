// Logical operator = use to combine or manuplate boolean values (true or false)

// and = &&
// or = ||
// not = !


const temp = 25;
if(temp>0 && temp <= 30 ){
    console.log("the weather is good")
}
else{
    console.log("the weather is bad")
}
